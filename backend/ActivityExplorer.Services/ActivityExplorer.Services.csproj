<Project Sdk="Microsoft.NET.Sdk">

  <ItemGroup>
    <ProjectReference Include="..\ActivityExplorer.Core\ActivityExplorer.Core.csproj" />
    <ProjectReference Include="..\ActivityExplorer.Data\ActivityExplorer.Data.csproj" />
  </ItemGroup>

  <ItemGroup>
    <!-- PS SDK and identity packages removed — PowerShell runs out-of-process via pwsh.exe -->
  </ItemGroup>

  <PropertyGroup>
    <TargetFramework>net9.0</TargetFramework>
    <ImplicitUsings>enable</ImplicitUsings>
    <Nullable>enable</Nullable>
    <CopyLocalLockFileAssemblies>true</CopyLocalLockFileAssemblies>
  </PropertyGroup>

  <ItemGroup>
    <InternalsVisibleTo Include="ActivityExplorer.Tests" />
  </ItemGroup>

</Project>
